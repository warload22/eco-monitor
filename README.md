# EcoMonitor - Система мониторинга экологической ситуации

Веб-приложение для мониторинга качества воздуха в Московском регионе с визуализацией данных на интерактивной карте.

## Возможности

- 🗺️ Интерактивная карта с мониторинговыми станциями
- 📊 Визуализация данных о качестве воздуха
- 📈 Статистика и отчеты с графиками
- 🔍 Фильтрация по параметрам, станциям и периодам
- 🎨 Цветовая индикация уровня загрязнения
- 📱 Адаптивный дизайн для мобильных устройств

## Отслеживаемые параметры

- **PM2.5** - Твердые частицы размером 2.5 микрон
- **PM10** - Твердые частицы размером 10 микрон
- **NO₂** - Диоксид азота
- **SO₂** - Диоксид серы
- **CO** - Угарный газ
- **O₃** - Озон

## Технологический стек

### Backend
- Python 3.10+
- Flask 3.0
- PostgreSQL
- psycopg2
- Pydantic

### Frontend
- Bootstrap 5
- OpenLayers
- Chart.js
- Vanilla JavaScript

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd eco_monitor
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE ecomonitor;
```

Примените схему базы данных:

```bash
psql -U postgres -d ecomonitor -f init_db.sql
```

Или выполните SQL из `models.py` напрямую в psql.

### 5. Настройка окружения

Скопируйте `.env.example` в `.env` и укажите свои параметры:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```
SECRET_KEY=your-secret-key-here
FLASK_DEBUG=True
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecomonitor
DB_USER=postgres
DB_PASSWORD=your_password
```

### 6. Инициализация базы данных

Запустите Python интерпретатор и выполните:

```python
from database import execute_query
from models import SQL_SCHEMA

# Выполните SQL схему для создания таблиц
# (или используйте migration tool)
```

### 7. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## Структура проекта

```
eco_monitor/
├── app.py                      # Точка входа приложения
├── config.py                   # Конфигурация
├── database.py                 # Управление БД
├── models.py                   # Модели данных
├── schemas.py                  # Pydantic схемы
├── routes/                     # Blueprints
│   ├── __init__.py
│   ├── api_measurements.py     # API эндпоинты
│   ├── map_view.py             # Страницы карты
│   └── reports.py              # Отчеты
├── static/
│   ├── css/
│   │   └── style.css
│   └── js/
│       ├── map.js              # Логика карты
│       └── filters.js          # Фильтры
├── templates/                  # HTML шаблоны
│   ├── base.html
│   ├── index.html
│   ├── report.html
│   └── about.html
├── utils/                      # Вспомогательные функции
│   ├── data_validation.py
│   └── geo_utils.py
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

## API Endpoints

### Measurements

- `GET /api/measurements` - Получить измерения (с фильтрами)
- `POST /api/measurements` - Создать новое измерение
- `GET /api/parameters` - Получить список параметров
- `GET /api/locations` - Получить список станций

### Reports

- `GET /reports/statistics` - Получить статистику
- `GET /reports/trends` - Получить данные временных рядов

## Примеры использования API

### Получение измерений

```bash
curl "http://localhost:5000/api/measurements?parameter_id=1&limit=10"
```

### Создание измерения

```bash
curl -X POST http://localhost:5000/api/measurements \
  -H "Content-Type: application/json" \
  -d '{
    "location_id": 1,
    "parameter_id": 1,
    "value": 25.5,
    "measured_at": "2026-01-25T12:00:00"
  }'
```

## Разработка

### Стиль кода

Проект следует принципам:
- Функциональное программирование
- Type hints для всех функций
- Guard clauses и early returns
- Чистое разделение ответственности

### Соглашения об именовании

- `snake_case` для файлов, переменных, функций
- Описательные имена с вспомогательными глаголами
- Параметризованные SQL запросы (безопасность)

## Лицензия

MIT License

## Автор

EcoMonitor Team © 2026
