# Инструкция по тестированию слоёв температуры и ветра

## Что было исправлено

### ✅ Температурный слой (текстовые подписи):
1. **Добавлено подробное логирование** - теперь можно отследить весь процесс загрузки
2. **Увеличены размеры** - текст с 16px до 20px, иконка с 1.2 до 1.5
3. **Убрано ограничение minZoom** - слой виден на всех масштабах для отладки
4. **Тестовые данные** - если API не возвращает данные, используются 9 тестовых точек для Москвы
5. **Улучшены уведомления** - понятные сообщения на русском языке

### ✅ Векторное поле ветра (большие стрелки):
1. **УВЕЛИЧЕНЫ стрелки** - базовый размер с 48px до 64px, все размеры увеличены на 33%
2. **Усилена анимация** - пульсация ±15% (было ±10%), мерцание 0.5-0.9 (было 0.6-0.9)
3. **Утолщены линии** - толщина стрелки с 4px до 6px, наконечник с 8px до 12px
4. **Добавлено подробное логирование** - отслеживание загрузки и отрисовки
5. **Тестовые данные** - 9 векторов ветра для Москвы, если нет реальных данных
6. **Увеличены размеры по скорости** - штиль 48px, сильный ветер 68px

### ✅ Управление масштабом:
1. **Температура видна на zoom 9-16** - средние и крупные масштабы
2. **Ветер виден на zoom 9-14** - средние масштабы (для больших стрелок)
3. **Автоматическое управление** - слои показываются/скрываются при изменении zoom
4. **Подсказки пользователю** - уведомления, если масштаб не подходящий

## Как протестировать

### Шаг 1: Запуск приложения

```bash
# Убедитесь, что вы в директории проекта
cd c:\Users\Admin\.cursor\projects\eco_monitor

# Активируйте виртуальное окружение (если есть)
.\.venv\Scripts\activate

# Запустите приложение
python run.py
```

### Шаг 2: Откройте приложение в браузере

1. Откройте браузер (Chrome/Firefox/Edge)
2. Перейдите на http://127.0.0.1:5000/
3. **ВАЖНО**: Откройте Developer Tools (нажмите F12)
4. Перейдите на вкладку **Console**

### Шаг 3: Проверка температурного слоя

1. В боковой панели найдите чекбокс **"🌡️ Температура (текстовые подписи)"**
2. **Включите чекбокс**
3. Наблюдайте в консоли:
   - `[TemperatureLabels] === НАЧАЛО ЗАГРУЗКИ ТЕМПЕРАТУРЫ ===`
   - `[TemperatureLabels] URL запроса: ...`
   - `[TemperatureLabels] Статус ответа: 200 OK` или `⚠️ НЕТ ДАННЫХ! Используем тестовые данные`
   - `[TemperatureLabels] ✅ Добавлено X features`
   - `[TemperatureLabels] === ЗАГРУЗКА ЗАВЕРШЕНА ===`

4. **На карте должны появиться:**
   - Цифры температуры (например, "+2.5°", "+3.2°") КРУПНЫМ шрифтом (20px)
   - Иконки термометров под цифрами
   - Белый фон с цветной рамкой вокруг текста
   - Цвет зависит от температуры:
     - Синий для холода (< 0°C)
     - Зелёный для комфорта (0-15°C)
     - Оранжевый для тепла (15-25°C)
     - Красный для жары (> 25°C)

5. **Проверка масштаба:**
   - Попробуйте изменить масштаб (колесико мыши)
   - Температура должна быть видна на zoom 9-16
   - Если zoom < 9 или > 16, появится уведомление: "💡 Измените масштаб до 9-16 для просмотра температуры"

### Шаг 4: Проверка векторов ветра

1. В боковой панели найдите чекбокс **"💨 Векторное поле ветра"**
2. **Включите чекбокс**
3. Наблюдайте в консоли:
   - `[Wind] === НАЧАЛО ЗАГРУЗКИ ВЕКТОРОВ ВЕТРА ===`
   - `[Wind] URL запроса: ...`
   - `[Wind] Статус ответа: 200 OK` или `⚠️ НЕТ ДАННЫХ! Используем тестовые данные`
   - `[Wind] ✅ Добавлено X features`
   - `[Wind] === ЗАГРУЗКА ЗАВЕРШЕНА ===`

4. **На карте должны появиться:**
   - **БОЛЬШИЕ стрелки** (размер 48-68px в зависимости от скорости ветра)
   - Стрелки **АНИМИРОВАННЫЕ** - плавно пульсируют и мерцают
   - Цвет зависит от скорости:
     - Серый (#d0d0d0) - штиль (< 1 м/с)
     - Голубой (#74add1) - лёгкий (1-3 м/с)
     - Синий (#4575b4) - слабый (3-6 м/с)
     - Оранжевый (#fdae61) - умеренный (6-10 м/с)
     - Оранжево-красный (#f46d43) - свежий (10-15 м/с)
     - Красный (#d73027) - сильный (> 15 м/с)

5. **Проверка направления:**
   - Стрелка показывает, **КУДА** дует ветер (не откуда!)
   - Наведите курсор на стрелку - должна появиться подсказка:
     - `💨 Ветер`
     - `Скорость: X.X м/с`
     - `Направление: Северо-западный (315°)`

6. **Проверка масштаба:**
   - Векторы ветра видны на zoom 9-14
   - Если zoom < 9 или > 14, появится уведомление: "💡 Измените масштаб до 9-14 для просмотра ветра"

### Шаг 5: Проверка анимации

1. **Анимация ветра должна работать постоянно:**
   - В консоли: `[Wind] Starting wind animation...`
   - Стрелки плавно пульсируют (изменяют размер)
   - Стрелки плавно мерцают (изменяют прозрачность)
   - Цикл анимации: 3 секунды

2. **Проверка производительности:**
   - Откройте вкладку **Performance** в DevTools
   - FPS должен быть стабильным (30-60 fps)
   - Если тормозит - уменьшите количество векторов в коде

### Шаг 6: Тестовые данные

Если в базе данных нет реальных данных, автоматически используются тестовые:

**Температура (9 точек):**
- Центр Москвы (Кремль): +2.5°C
- Запад: +1.8°C
- Север: +3.2°C
- Юг: +2.1°C
- Восток: +2.8°C
- И ещё 4 точки в углах

**Ветер (9 векторов):**
- Центр: 5.2 м/с, 270° (западный)
- Запад: 4.8 м/с, 290° (северо-западный)
- Север: 6.1 м/с, 240° (юго-западный)
- И т.д.

### Шаг 7: Проверка уведомлений

1. При включении слоя должно появиться уведомление в правом верхнем углу:
   - `✅ Загружено X меток температуры`
   - `✅ Загружено X векторов ветра`

2. Если используются тестовые данные:
   - `⚠️ Используются тестовые данные температуры`
   - `⚠️ Используются тестовые данные ветра`

3. При ошибке загрузки:
   - `❌ Ошибка загрузки температуры`
   - `❌ Ошибка загрузки ветра`

## Что смотреть в консоли

### Инициализация (при загрузке страницы):
```
[WeatherLayers] ========================================
[WeatherLayers] ИНИЦИАЛИЗАЦИЯ ПОГОДНЫХ СЛОЁВ
[WeatherLayers] ========================================
[WeatherLayers] Map object: ✅ OK
[WeatherLayers] Создание температурного слоя...
[WeatherLayers] Температурный слой: ✅ Создан
[WeatherLayers] Создание слоя векторов ветра...
[WeatherLayers] Слой ветра: ✅ Создан
...
[WeatherLayers] ✅ ИНИЦИАЛИЗАЦИЯ ЗАВЕРШЕНА УСПЕШНО
```

### Загрузка температуры (при включении чекбокса):
```
[TemperatureLabels] === НАЧАЛО ЗАГРУЗКИ ТЕМПЕРАТУРЫ ===
[TemperatureLabels] Границы карты: {...}
[TemperatureLabels] Текущий zoom: 10
[TemperatureLabels] URL запроса: /api/weather/map-grid?...
[TemperatureLabels] Статус ответа: 200 OK
[TemperatureLabels] Количество точек: 9
[TemperatureLabels] ✅ Добавлено 9 features
[TemperatureLabels] === ЗАГРУЗКА ЗАВЕРШЕНА ===
```

### Загрузка ветра (при включении чекбокса):
```
[Wind] === НАЧАЛО ЗАГРУЗКИ ВЕКТОРОВ ВЕТРА ===
[Wind] Границы карты: {...}
[Wind] Текущий zoom: 10
[Wind] URL запроса: /api/weather/wind-vectors?...
[Wind] Статус ответа: 200 OK
[Wind] Количество векторов: 9
[Wind] ✅ Добавлено 9 features
[Wind] === ЗАГРУЗКА ЗАВЕРШЕНА ===
```

## Возможные проблемы и решения

### Проблема 1: Ничего не видно на карте
**Решение:**
1. Проверьте консоль - есть ли ошибки?
2. Проверьте zoom - должен быть 9-16 для температуры, 9-14 для ветра
3. Проверьте, что чекбоксы включены
4. Проверьте, что features добавлены: `Всего в источнике: X features`

### Проблема 2: Слои есть, но не анимируются
**Решение:**
1. Проверьте консоль: `[Wind] Starting wind animation...`
2. Проверьте FPS - возможно, браузер тормозит
3. Попробуйте обновить страницу (F5)

### Проблема 3: Направление ветра неправильное
**Решение:**
1. Помните: стрелка показывает, КУДА дует ветер (не откуда)
2. Северный ветер (0°) дует С СЕВЕРА НА ЮГ - стрелка смотрит на юг
3. Западный ветер (270°) дует С ЗАПАДА НА ВОСТОК - стрелка смотрит на восток

### Проблема 4: API возвращает пустой массив
**Решение:**
- Автоматически используются тестовые данные
- В консоли: `⚠️ НЕТ ДАННЫХ! Используем тестовые данные для Москвы`
- Тестовые данные должны отображаться на карте

### Проблема 5: Слишком много/мало стрелок
**Решение:**
- Измените параметр `grid_size` в функции `загрузитьВекторыВетра`
- Текущее значение: 10 (оптимально для больших стрелок)
- Можно уменьшить до 8 или увеличить до 12

## Параметры настройки

### Температурный слой (в weatherLayers.js):
- Размер шрифта: `font: 'bold 20px ...'` (строка 107)
- Размер иконки: `scale: 1.5` (строка 130)
- Смещение текста: `offsetY: -12` (строка 116)
- Плотность точек: `grid_size: 8` (строка 175)

### Векторы ветра (в weatherLayers.js):
- Базовый размер стрелки: `размер = 64` (строка 280)
- Размеры по скорости: `базовыйРазмер = 48-68` (строки 305-327)
- Амплитуда пульсации: `* 0.15` (строка 332)
- Амплитуда мерцания: `* 0.2` (строка 341)
- Плотность векторов: `grid_size: 10` (строка 425)

### Диапазоны zoom (в map.js):
- Температура: `zoom >= 9 && zoom <= 16` (строка 91)
- Ветер: `zoom >= 9 && zoom <= 14` (строка 104)

## Финальная проверка

- [  ] Температурные подписи отображаются
- [  ] Цифры крупные и читаемые (20px)
- [  ] Иконки термометров видны
- [  ] Стрелки ветра БОЛЬШИЕ (видны сразу)
- [  ] Стрелки анимированные (пульсируют и мерцают)
- [  ] Направление стрелок корректное (куда дует ветер)
- [  ] Tooltip появляется при наведении на стрелку
- [  ] Слои скрываются при zoom < 9 или > 14/16
- [  ] Уведомления появляются при включении слоёв
- [  ] Тестовые данные работают при отсутствии реальных
- [  ] В консоли подробное логирование
- [  ] Нет ошибок в консоли

## Коммит

После успешного тестирования создан коммит:
```
fix: экстренный ремонт слоёв температуры и ветра

- Температурные лейблы: увеличены размеры (20px текст, 1.5 иконка), добавлено логирование, тестовые данные
- Ветер: БОЛЬШИЕ стрелки (48-68px), усиленная анимация пульсации и мерцания
- Управление масштабом: температура 9-16, ветер 9-14
- Подробное логирование для диагностики
- Тестовые данные для Москвы при отсутствии реальных
```
